# Copy this file to .env and fill in real values:
#   cp .env.example .env
#
# .env is gitignored — it never gets committed.

OPENROUTER_API_KEY=sk-or-v1-your-key-here
POSTGRES_PASSWORD=choose-your-own-local-password
